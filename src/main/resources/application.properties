spring.application.name=spring-mcp-ordertools

# --- MCP server ---
spring.ai.mcp.server.name=order-tools
spring.ai.mcp.server.version=0.0.1
# WebMVC transport exposes SSE at /sse and messages at /mcp/message
spring.ai.mcp.server.type=SYNC

server.port=8080